---
users:
{% for i in range(1,121) %}
  - name: appuser{{ i }}
    password: "Password@123"
    description: "Application User {{ i }}"
    groups:
      - Users
    password_never_expires: true
    
